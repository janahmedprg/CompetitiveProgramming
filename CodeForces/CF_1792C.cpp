#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int T;
    cin >> T;
    while (T--)
    {
        int n;
        cin>>n;
        vector<int> v(n);
        int c =0;
        for(int i = 0; i<n;++i){
            cin>>v[i];
        }
        for(int i=0; i<n/2;++i) {
            
        }
        cout<<c<<"\n";
    }
    return 0;
}